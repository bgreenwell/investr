% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/invest_survfit.R
\name{invest.survfit}
\alias{invest.survfit}
\alias{invest.Surv}
\title{Inverse estimation for Kaplan-Meier survival curves}
\usage{
\method{invest}{survfit}(object, y0 = 0.5, level = object$conf.int, ...)

\method{invest}{Surv}(object, y0 = 0.5, level = 0.95, ...)
}
\arguments{
\item{object}{An object that inherits from class
\code{"survfit"} (a fitted Kaplan-Meier curve from
\code{survival::survfit()}) or class \code{"Surv"} (a censored response
object from \code{survival::Surv()}, for which a Kaplan-Meier curve is fit
internally).}

\item{y0}{A numeric scalar in (0, 1) giving the survival probability at
which to invert the curve. Default is \code{0.5} (i.e., the median
survival time).}

\item{level}{A numeric scalar between 0 and 1 giving the confidence level
for the interval. For \code{"survfit"} objects, this must match the
\code{conf.int} level the curve was fit with (the default); refit with
\code{survival::survfit(..., conf.int = level)} for a different level. For
\code{"Surv"} objects, the internal fit uses \code{level} directly.}

\item{...}{Additional optional arguments. At present, no optional
arguments are used.}
}
\value{
An object of class \code{"invest"} containing the components
\code{estimate}, \code{lower}, \code{upper}, and \code{interval} (always
\code{"inversion"}). Components are \code{NA} whenever the survival curve
(or a confidence limit) never drops below \code{y0}; in particular,
\code{upper} is frequently \code{NA} in small samples, meaning the upper
confidence limit is indeterminate (infinite).
}
\description{
Inverts a Kaplan-Meier estimate of the survival function: given a survival
probability \code{y0}, these methods estimate the survival time \eqn{t} at which
\eqn{S(t) = y_0}, along with a confidence interval obtained by inverting
the pointwise confidence band for the survival curve (the same inversion
idea used by \code{\link[=invest]{invest()}} for regression models). For example, the default
\code{y0 = 0.5} gives the median survival time.
}
\note{
These methods only support a single survival curve; for a
\code{"survfit"} object with multiple strata, invert each stratum's curve
separately (e.g., via \code{survfit[i]} subscripting).
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {

  # Median survival time (with 95\% confidence limits) for the acute
  # myelogenous leukemia data
  km <- survival::survfit(survival::Surv(time, status) ~ 1,
                          data = survival::aml)
  invest(km)  # y0 = 0.5 is the median

  # Same, but fitting the curve internally from a censored response
  invest(survival::Surv(survival::aml$time, survival::aml$status))

  # Time at which 75\% survival is reached
  invest(km, y0 = 0.75)

}
}
\seealso{
The \code{quantile.survfit} method in the \pkg{survival} package,
which these methods use for the inversion and which expresses the same
calculation in terms of quantiles of the event-time distribution (i.e.,
\code{probs = 1 - y0}).
}
